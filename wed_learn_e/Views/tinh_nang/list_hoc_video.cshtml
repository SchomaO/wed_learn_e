@using PagedList.Mvc;
@model PagedList.IPagedList<wed_learn_e.Models.bai_giang_video>

@{
    ViewBag.Title = "Danh Sách Bài Luyện Nghe Video";
    Layout = "~/Views/Shared/_Layoutcauhoi.cshtml";
}

<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />

<style>
    body {
        margin: 0;
        font-family: Arial, sans-serif;
        background: #f4f9ff;
    }

    .list-container {
        max-width: 900px;
        margin: 40px auto;
        padding: 0 20px;
    }

    .page-title {
        text-align: center;
        color: #1a73e8;
        font-size: 32px;
        font-weight: bold;
        margin-bottom: 30px;
    }

    /* CSS THANH TÌM KIẾM */
    .search-box {
        background: white;
        padding: 20px;
        border-radius: 12px;
        box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        margin-bottom: 25px;
        display: flex;
        gap: 10px;
    }

    .search-input {
        flex: 1;
        padding: 12px 15px;
        border: 1px solid #ddd;
        border-radius: 8px;
        font-size: 16px;
        outline: none;
        transition: 0.3s;
    }

        .search-input:focus {
            border-color: #1a73e8;
            box-shadow: 0 0 5px rgba(26,115,232,0.3);
        }

    .btn-search {
        background: #1a73e8;
        color: white;
        border: none;
        padding: 0 25px;
        border-radius: 8px;
        font-weight: bold;
        cursor: pointer;
        transition: 0.3s;
        white-space: nowrap;
    }

        .btn-search:hover {
            background: #1557b0;
        }

    /* CSS THẺ BÀI HỌC */
    .lesson-card {
        background: white;
        border-radius: 12px;
        padding: 20px 25px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        margin-bottom: 20px;
        border-left: 6px solid #1a73e8;
        transition: 0.3s;
    }

        .lesson-card:hover {
            transform: translateX(10px);
            box-shadow: 0 6px 15px rgba(0,0,0,0.1);
            border-left-color: #ff9800;
        }

    .lesson-info {
        display: flex;
        align-items: center;
        gap: 20px;
    }

    .lesson-icon {
        font-size: 35px;
        background: #eef7ff;
        width: 60px;
        height: 60px;
        display: flex;
        justify-content: center;
        align-items: center;
        border-radius: 50%;
    }

    .lesson-details h3 {
        margin: 0 0 5px 0;
        font-size: 20px;
        color: #333;
    }

    .lesson-details p {
        margin: 0;
        color: #777;
        font-size: 14px;
    }

    .btn-play {
        background: #1a73e8;
        color: white;
        padding: 10px 20px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: bold;
        transition: 0.2s;
        white-space: nowrap;
    }

        .btn-play:hover {
            background: #1557b0;
        }

    .empty-msg {
        text-align: center;
        color: #dc3545;
        font-size: 18px;
        background: white;
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        font-weight: bold;
    }

    /* CSS PHÂN TRANG */
    .pagination-container {
        text-align: center;
        margin-top: 30px;
    }

    .pagination {
        display: inline-flex;
        padding-left: 0;
        list-style: none;
        border-radius: 0.25rem;
    }
</style>

<div class="list-container">
    <div class="page-title">
        🎬 Luyện học qua video: @ViewBag.TenCapDo
    </div>

    @using (Html.BeginForm("list_hoc_video", "tinh_nang", FormMethod.Get))
    {
        <div class="search-box">
            <input type="hidden" name="id_cap_do" value="@ViewBag.IdCapDo" />

            <input type="text" name="searchString" value="@ViewBag.CurrentFilter" class="search-input" placeholder="Nhập tên bài video bạn muốn tìm kiếm..." />
            <button type="submit" class="btn-search">🔍 Tìm Kiếm</button>
        </div>
    }

    @if (Model == null || Model.Count == 0)
    {
        <div class="empty-msg">
            Hiện tại chưa có bài video nào phù hợp với tìm kiếm của bạn.
        </div>
    }
    else
    {
        // Thuật toán để số thứ tự tiếp nối khi sang trang 2, 3...
        int stt = (Model.PageNumber - 1) * Model.PageSize + 1;

        foreach (var item in Model)
        {
            <div class="lesson-card">
                <div class="lesson-info">
                    <div class="lesson-icon">
                        ▶️
                    </div>
                    <div class="lesson-details">
                        <h3>Bài @stt: @item.tieu_de</h3>
                        <p>⏱ Thời lượng: @item.thoi_luong_phut phút</p>
                    </div>
                </div>

                <a href="@Url.Action("bai_video", "tinh_nang", new { id_video = item.id_video })" class="btn-play">
                    Vào học
                </a>
            </div>
            stt++;
        }
    }

    <div class="pagination-container">
        <div style="margin-bottom: 10px; color: #666; font-weight: bold;">
            Trang @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) / @Model.PageCount
        </div>

        @Html.PagedListPager(Model, page => Url.Action("list_hoc_video",
            new { page, searchString = ViewBag.CurrentFilter, id_cap_do = ViewBag.IdCapDo }))
    </div>
</div>